看你这张图，**右边昨天那一整套 `try / catch / finally` 不要再复制进去**。因为你左边现有脚本本来已经有完整的 `try → catch → finally`，最合适的是在现有结构上做最小修改。

你现在主要改 3 个地方就够了。

### ① 脚本最前面：加日志初始化 + Firefox 进程变量

放在你现有最外层 `try {` **之前**：

```powershell
# ----------------------------------------
# ログ設定
# ----------------------------------------

$LogDir = "C:\temp\dataiku_logs"

if (-not (Test-Path $LogDir)) {
    New-Item `
        -ItemType Directory `
        -Path $LogDir `
        -Force |
        Out-Null
}

$LogFile = Join-Path `
    $LogDir `
    ("dataiku_" + (Get-Date -Format "yyyyMMdd_HHmmss") + ".log")

Start-Transcript `
    -Path $LogFile `
    -Append

Write-Host "===== Dataiku Screenshot Start ====="
Write-Host "Start Time : $(Get-Date)"
Write-Host "User       : $env:USERNAME"

# 今回起動した Firefox のプロセスを保持
$firefoxProcess = $null


try {

    # ここから既存処理
```

也就是说右边截图的：

```powershell
try {
    # ここに既存処理
}
catch {
...
}
finally {
...
}
```

**不要再加一套。**

---

### ② Firefox 启动这里：保存自己启动的 PID

你现在应该类似：

```powershell
Start-Process `
    -FilePath $FirefoxPath `
    -ArgumentList @(
        "--headless"
    )
```

改成：

```powershell
$firefoxProcess = Start-Process `
    -FilePath $FirefoxPath `
    -ArgumentList @(
        "--headless"
    ) `
    -PassThru

Write-Host "    Firefox PID: $($firefoxProcess.Id)"
```

关键只是：

```powershell
$firefoxProcess =
```

和：

```powershell
-PassThru
```

这样 PowerShell 就知道：

> 这一只 Firefox 是我这次任务启动的。

---

### ③ 你左边最后的 Firefox 清理要改

你现在左边最后大约 878～882 行是：

```powershell
# Firefox プロセスを停止
Get-Process firefox `
    -ErrorAction SilentlyContinue |
    Stop-Process -Force
```

**这一段删掉。**

因为它会：

```text
所有 firefox.exe
→ 全部强制关闭
```

有可能影响正在使用 Firefox 的其他用户。

改成：

```powershell
# 今回起動した Firefox のみ終了
if ($firefoxProcess) {

    try {

        if (-not $firefoxProcess.HasExited) {

            Write-Host "    Firefox を終了します"
            Write-Host "    PID: $($firefoxProcess.Id)"

            Stop-Process `
                -Id $firefoxProcess.Id `
                -Force `
                -ErrorAction SilentlyContinue
        }

    }
    catch {

        # 終了処理中のエラーは無視
    }
}
```

然后在 `finally` **最末尾**加日志结束：

```powershell
Write-Host "End Time : $(Get-Date)"
Write-Host "===== Dataiku Screenshot End ====="

try {
    Stop-Transcript
}
catch {
}
```

所以你左边现有的 `finally` 最终应该大致长这样：

```powershell
finally {

    # ----------------------------------------
    # 終了処理
    # ----------------------------------------

    # Marionette Session を終了
    if ($stream) {

        try {

            Invoke-MarionetteCommand `
                -Stream $stream `
                -Command "WebDriver:DeleteSession" `
                -Parameters @{} |
                Out-Null
        }
        catch {

            # 終了処理中のエラーは無視
        }
    }


    # Stream を終了
    if ($stream) {
        $stream.Dispose()
    }


    # TCP 接続を終了
    if ($client) {
        $client.Close()
    }


    # 今回起動した Firefox のみ終了
    if ($firefoxProcess) {

        try {

            if (-not $firefoxProcess.HasExited) {

                Write-Host "    Firefox を終了します"
                Write-Host "    PID: $($firefoxProcess.Id)"

                Stop-Process `
                    -Id $firefoxProcess.Id `
                    -Force `
                    -ErrorAction SilentlyContinue
            }

        }
        catch {

            # 終了処理中のエラーは無視
        }
    }


    Write-Host "End Time : $(Get-Date)"
    Write-Host "===== Dataiku Screenshot End ====="

    try {
        Stop-Transcript
    }
    catch {
    }
}
```

还有一个地方也要同步改：你前一张图脚本开头有：

```powershell
Get-Process firefox `
    -ErrorAction SilentlyContinue |
    Stop-Process -Force
```

**开头这一段也建议删除。**

否则虽然结尾改安全了，但每次 Task 开始的时候还是会把其他人的 Firefox 全关掉。

最终逻辑就变成：

```text
以前

Task 开始
↓
杀掉所有 Firefox   ← 风险
↓
启动自己的 Firefox
↓
处理
↓
杀掉所有 Firefox   ← 风险
```

改成：

```text
Task 开始
↓
不碰现有 Firefox
↓
启动自己的 Headless Firefox
↓
记录 PID
↓
处理
↓
只结束这次启动的 Firefox
```

这才比较适合作为正式后台任务。

另外你右边昨天加的 `Start-Transcript` 思路没问题，只是**不要再套第二层 try/catch/finally**。直接融合进你左边现在已经存在的结构就行。