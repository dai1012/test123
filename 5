对，这个现象基本能定位了：**不是 Marionette 卡住，而是 Firefox 的“已有实例复用”机制在干扰。**

你现在提前打开了另一个 Profile 的普通 Firefox，然后脚本执行：

```powershell
Start-Process `
    -FilePath $FirefoxPath `
    -ArgumentList @(
        "--headless"
    )
```

Firefox 默认会先检查**同一 Windows 用户下是否已经存在 Firefox 实例**。如果有，新的 `firefox.exe` 不一定真的独立启动一个全新的浏览器进程，而可能尝试把启动请求交给已有实例。

这就解释了你现在的现象：

```text
普通 Firefox 已经打开
        ↓
脚本启动 firefox.exe --headless
        ↓
Firefox 发现已有实例
        ↓
尝试复用/通信
        ↓
Headless + Marionette 没有按预期独立启动
        ↓
PowerShell 一直等 127.0.0.1:2828
```

你截图里也正好停在：

```text
[2] Firefox Headless + Marionette を起動します
Firefox PID: 1484
```

后面 Marionette 端口一直没起来。

## 应该怎么改

正式版自动化 Firefox 应该加：

```text
-no-remote
```

它的意思就是：

> 不要连接/复用已经运行的 Firefox，给我启动一个独立 Firefox 实例。

如果你自动化还使用专用 Profile，我建议启动代码直接改成这样：

```powershell
Write-Host "[2] Firefox Headless + Marionette を起動します"

$env:MOZ_MARIONETTE = "1"

$firefoxProcess = Start-Process `
    -FilePath $FirefoxPath `
    -ArgumentList @(
        "--headless",
        "-no-remote",
        "-profile",
        "`"$FirefoxProfilePath`""
    ) `
    -PassThru

Write-Host "    Firefox PID: $($firefoxProcess.Id)"
```

假设你前面已经定义：

```powershell
$FirefoxProfilePath = "C:\Users\xxxxx\AppData\Roaming\Mozilla\Firefox\Profiles\xxxxx.infra_4_tai"
```

那就可以。

如果你原本启动参数里已经有 Profile，就只需要增加：

```powershell
"-no-remote",
```

例如原来：

```powershell
-ArgumentList @(
    "--headless",
    "-profile",
    "`"$FirefoxProfilePath`""
)
```

改成：

```powershell
-ArgumentList @(
    "--headless",
    "-no-remote",
    "-profile",
    "`"$FirefoxProfilePath`""
)
```

---

### 为什么之前 `Stop-Process firefox` 的时候没发现这个问题

这也正好解释了。

原先脚本开始先：

```powershell
Get-Process firefox |
    Stop-Process -Force
```

所以运行脚本时：

```text
所有 Firefox 被关闭
↓
系统里没有已有 Firefox 实例
↓
Headless Firefox 正常独立启动
↓
Marionette 2828 正常启动
```

因此一直没暴露 Firefox 单实例复用的问题。

现在我们为了**不影响其他用户**把：

```powershell
Stop-Process firefox
```

删掉以后，就出现了：

```text
已经有普通 Firefox
+
自动化 Firefox
```

这种真正生产环境会遇到的场景。

所以这次测试非常有价值，正好发现了一个正式化前必须处理的问题。

---

## 最终应该做到这种隔离

理想状态：

```text
普通用户 Firefox
Profile: default-release
GUI
           │
           │ 完全独立
           │
自动化 Firefox
Profile: infra_4_tai
Headless
-no-remote
Marionette :2828
```

这样别人可以正常：

```text
Firefox GUI
→ Google
→ Dataiku
→ 其他网站
```

同时你的 Task Scheduler：

```text
Firefox Headless
→ Dataiku Usage
→ Screenshot
```

两边互不影响。

---

## 还要注意一个事情：自动化专用 Profile 自己不能同时打开

`-no-remote` 能解决：

> **其他 Profile 已经打开**

但不能解决：

> **同一个 `infra_4_tai` Profile 已经被另一个 Firefox 使用**

例如：

```text
普通 Firefox
default-release     ← OK

自动脚本
infra_4_tai         ← OK
```

没问题。

但是：

```text
有人手动打开
infra_4_tai
        ↓
自动脚本又启动
infra_4_tai
```

仍然可能报：

```text
Firefox is already running
Profile in use
```

所以最好明确：

> `infra_4_tai` は自動処理専用Profileとし、手動操作では使用しない。

---

## 还有 Marionette 2828

普通 Firefox 一般不会占：

```text
127.0.0.1:2828
```

所以普通用户 Firefox 和你自动化 Firefox 同时存在没有问题。

真正有问题的是两个自动化任务同时运行：

```text
Automation A
Marionette 2828

Automation B
Marionette 2828
          ↑
          冲突
```

因此 Task Scheduler 里继续保持：

> **タスクが既に実行中の場合：新しいインスタンスを開始しない**

就很好。

---

### 你现在可以直接做这个验证

先保留左边已经打开的普通 Firefox，不要关闭。

给自动化启动参数加：

```powershell
"-no-remote",
```

然后重新运行 `.ps1`。

预期应该变成：

```text
普通 Firefox GUI
→ 继续正常使用

PowerShell
→ Firefox Headless 独立启动
→ Marionette 2828 OK
→ Dataiku 登录
→ Screenshot
→ 自动化 Firefox 结束

普通 Firefox GUI
→ 从头到尾都不会被关闭
```

**如果这个测试成功，那么“脚本运行时不会影响其他用户正常使用 Firefox”这一点就基本验证出来了。**

会议上这部分也可以很好地说明：

> 他のFirefoxセッションへの影響を避けるため、自動処理では専用Profileと `-no-remote` オプションを使用し、既存のFirefoxとは独立したHeadlessインスタンスとして起動しています。